function reset() { //Reset function - if click on "C" button
	
	disp.value = "";
	
}
function clickN(num) { //Reset function - if click on "C" button
	/*var num = k8.value;*/
	if (disp.value.length<12) {  
			disp.value = disp.value + num;
	}
	else {}
}
